apiVersion: v1
kind: Service
metadata:
  name: agent-warden-portal
  namespace: agent-warden-system
  labels:
    app.kubernetes.io/name: agent-warden-portal
    app.kubernetes.io/part-of: agent-warden
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-warden-portal
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
