# TODOs — 2026-03-14 Session

## Priority 1: Fix Azure OpenAI End-to-End Call

The model config is correct (`openai/gpt-4o`), the DLP proxy has MI bearer token auth, and the RBAC role is assigned. The remaining issue is that OpenClaw's embedded CLI agent bypasses the proxy and sends the API key directly to Azure OpenAI (which rejects it because `disableLocalAuth=true`).

### Steps:
1. **Test via gateway process** — Instead of `openclaw agent` CLI, test through the running gateway using `curl` to the gateway API or the Control UI. The gateway process should respect `models.providers.openai.baseUrl` and route through the proxy.
   ```bash
   # Port-forward (or exec into pod) and test gateway API
   curl -X POST http://127.0.0.1:18789/api/agent \
     -H "Authorization: Bearer <gateway-token>" \
     -H "Content-Type: application/json" \
     -d '{"message":"say hello","agentId":"main"}'
   ```

2. **If gateway also sends API key directly**: Configure OpenClaw's auth-profiles to not include an API key, since the proxy injects the MI bearer token. Try setting `OPENAI_API_KEY` to a dummy value like `managed-identity` — the proxy replaces it anyway.

3. **Verify proxy intercepts**: Check proxy logs after a gateway-routed request to confirm it's acquiring MI tokens and replacing the Authorization header.

4. **Make `openclaw.json` persistent via Helm**: Add a ConfigMap or init-container that seeds `openclaw.json` on first boot (or on Helm upgrade). Currently it's manually written to the PVC.

## Priority 2: Deploy agent-warden-server

The MCP control plane server for tenant lifecycle management.

### Steps:
1. **Create Dockerfile** for `agent-warden-server/`
2. **Fix TypeScript build issues** (Graph client imports, Cosmos query params, K8s client API)
3. **Build and push** to ACR: `az acr build --registry acragentwardendev --image agent-warden-server:latest`
4. **Create K8s Deployment manifest** for `agent-warden-system` namespace
5. **Configure env vars**:
   - `AZURE_COSMOS_ENDPOINT` = `https://cosmos-agentwarden-dev.documents.azure.com:443/`
   - `AZURE_COSMOS_DATABASE` = `agent-warden`
   - `AZURE_KEYVAULT_URL` = `https://kv-plat-agentwarden-dev.vault.azure.net/`
   - `AZURE_PURVIEW_ENDPOINT` = `https://pview-agentwarden-dev.purview.azure.com`
   - `AKS_CLUSTER_NAME` = `aks-agentwarden-dev`
   - `AKS_RESOURCE_GROUP` = `rg-agentwarden-dev`
   - `ACR_LOGIN_SERVER` = `acragentwardendev.azurecr.io`
   - `HELM_CHART_PATH` = TBD
6. **Deploy** to `agent-warden-system` namespace
7. **Test MCP tools**: `warden.tenant.provision`, `warden.health.check`, etc.

## Priority 3: Remaining Deployment Items

- [ ] **Gateway API / Ingress**: Configure TLS certificate, DNS record, HTTPRoute for external access
- [ ] **OpenClaw config persistence**: Seed `openclaw.json` from Helm ConfigMap/init-container
- [ ] **Helm chart**: Add `openclaw.json` ConfigMap template with model/provider config
- [ ] **CI/CD pipelines**: Verify GitHub Actions workflows
- [ ] **Security hardening**: Review NetworkPolicy egress rules, enable Defender scanning
- [ ] **Monitoring**: Verify Log Analytics integration, set up alerts
- [ ] **Multi-tenant testing**: Provision a second tenant to validate isolation

## Notes

- Private AKS: All kubectl/helm via `az aks command invoke`
- Helm deploy dir: `~/helm-deploy`
- OpenClaw docs: https://docs.openclaw.ai/install/kubernetes, https://docs.openclaw.ai/gateway/configuration-reference
- Gateway token: `<set via kubectl exec or Helm values>`
- The subscription enforces Entra-only auth on Cognitive Services — cannot enable API key auth
